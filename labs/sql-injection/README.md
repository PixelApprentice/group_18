SQL Injection Lab
-----------------

This minimal Express app demonstrates SQL Injection using SQLite.

How to run (from project root):

1. Build and start the docker compose stack:

   docker compose up --build nginx lab_sql_injection

2. Open http://localhost/lab/sql-injection and try queries.

Note: This app is intentionally vulnerable and isolated in Docker. Do not expose it publicly.
